// Incomplete

#include <stdio.h>

int main ()
{
    // Declaring the marks array first and then defining the 
    // elements one by one
    int i, marks[5];
    marks[0] = 80;
    marks[1] = 60;
    marks[2] = 70;
    marks[3] = 85;
    marks[4] = 75;
    for (i=0; i<5; i++)
    {
        printf("Marks [%d] = %d\n", i, marks[i]);
    }

    // Declaring and defining the array 
    int j, marks2[5] = {80, 60, 70, 85, 75};

    //Declaring and defining the array without 

}